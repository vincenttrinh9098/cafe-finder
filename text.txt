To safetly pull from main from local branch:

1) if you want your local changes on top of pulling from main:

git fetch origin
git rebase origin/main


2) If there are conflicts:

Git will stop and tell you which files conflict
Fix them manually
Then run:
git add .
git rebase --continue